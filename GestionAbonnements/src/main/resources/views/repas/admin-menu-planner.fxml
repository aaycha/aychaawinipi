<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.gestion.ui.repas.AdminMenuPlannerController"
            stylesheets="@../../styles/restoration-2026.css"
            styleClass="modern-2026-root">
    
    <top>
        <VBox spacing="10" styleClass="glass-panel" style="-fx-padding: 12; -fx-background-radius: 0 0 15 15;">
            <HBox alignment="CENTER_LEFT" spacing="20">
                <Button text="←" styleClass="btn-secondary" style="-fx-background-radius: 50%; -fx-font-size: 10px;" onAction="#onBack"/>
                <Label text="Planificateur de Menus Hebdomadaire" style="-fx-font-size: 18px; -fx-font-weight: 900; -fx-text-fill: #1E3A8A;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <HBox spacing="8" alignment="CENTER_LEFT">
                    <Button text="Semaine Précédente" styleClass="btn-secondary" style="-fx-font-size: 10px;" onAction="#onPreviousWeek"/>
                    <Label fx:id="currentWeekLabel" text="Semaine du 16 au 22 Février 2026" style="-fx-font-weight: bold; -fx-font-size: 11px;"/>
                    <Button text="Semaine Suivante" styleClass="btn-primary" style="-fx-font-size: 10px;" onAction="#onNextWeek"/>
                </HBox>
            </HBox>
        </VBox>
    </top>

    <center>
        <ScrollPane fitToWidth="false" fitToHeight="false" hbarPolicy="AS_NEEDED" vbarPolicy="AS_NEEDED" style="-fx-background-color: transparent;" styleClass="modern-scroll">
            <VBox spacing="12" style="-fx-padding: 15;">
                <GridPane fx:id="plannerGrid" hgap="15" vgap="15">
                    <columnConstraints>
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                        <ColumnConstraints percentWidth="14.28" />
                    </columnConstraints>
                    
                    <!-- Days Headers will be dynamically updated or fixed -->
                </GridPane>
            </VBox>
        </ScrollPane>
    </center>

    <right>
        <VBox fx:id="dishPalette" spacing="15" prefWidth="300" styleClass="admin-sidebar" style="-fx-background-radius: 32 0 0 32; -fx-border-width: 0 0 0 1.5;">
            <padding>
                <Insets top="30" right="20" bottom="30" left="20"/>
            </padding>
            <Label text="CATALOGUE RAPIDE" styleClass="sidebar-title" />
            <TextField fx:id="searchField" promptText="Rechercher un plat..." styleClass="search-bar-smart" />
            <ListView fx:id="availableDishesList" VBox.vgrow="ALWAYS" styleClass="modern-list-view" />
            <Button text="Nouveau Plat" styleClass="btn-turquoise" maxWidth="Infinity" />
        </VBox>
    </right>

</BorderPane>
